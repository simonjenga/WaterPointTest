package com.water.point;

import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.junit.runners.JUnit4;

import com.water.point.Application;

/**
 * Test case for {@link Application}.
 * 
 * @author Simon Njenga
 * @since 0.1
 */
@RunWith(JUnit4.class)
public class ApplicationTest {

    /**
     * JUnit tests to be implemented later!.
     * The results for this test are logged to console.
     * 
     * @throws Exception If some problem inside
     */
    @Test
    public void testToBeImplementedLater() throws Exception {
    	Assert.assertTrue("Tests to be implemented later!".length() > 0);
    }
}
